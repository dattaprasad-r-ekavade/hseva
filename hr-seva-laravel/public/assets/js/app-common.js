/* HR Seva shared portal scripts — load order: core → portal */
(function () {
  "use strict";
  if (window.HRCommon && window.HRCommon.__portalBootstrapped) return;
  if (window.HRCommon) window.HRCommon.__portalBootstrapped = true;
})();
