{
  "name": "hr-seva-qa",
  "private": true,
  "scripts": {
    "test": "playwright test --project=smoke",
    "test:tour": "playwright test --project=onboarding-tour",
    "tour:gif": "bash scripts/make-tour-gif.sh",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
